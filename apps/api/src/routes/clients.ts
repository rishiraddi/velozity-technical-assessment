import {Router} from 'express'; import {z} from 'zod'; import {Role} from '@prisma/client'; import {prisma} from '../lib/prisma.js'; import {requireAuth,requireRole} from '../middleware/auth.js'; const r=Router();r.use(requireAuth);r.get('/',requireRole(Role.ADMIN,Role.PM),async(_req,res,next)=>{try{res.json({clients:await prisma.client.findMany({orderBy:{name:'asc'}})})}catch(e){next(e)}});r.post('/',requireRole(Role.ADMIN),async(req,res,next)=>{try{const b=z.object({name:z.string().min(2),email:z.string().email().optional()}).parse(req.body);res.status(201).json({client:await prisma.client.create({data:b})})}catch(e){next(e)}});export default r;
